services:
  pentool:
    build: .
    volumes:
      - ./:/app          # bind mount for development (keeps source in sync)
    working_dir: /app
    tty: true
    # network_mode: "host"   # optional: use for local network scanning (linux only)
    # command: ["python", "src/scanner.py"]